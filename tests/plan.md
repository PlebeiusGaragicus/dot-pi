test prompt for 'dot-mi' repo:

Please explore the shared/extensions directory.  I'm considering have all extensions live inside a directory with their name, then using index.ts for the extension code and adding a README.md so that each extension can be fully documented.