import type { ExtensionAPI } from "@mariozechner/pi-coding-agent";

export default function (_pi: ExtensionAPI) {
	// Browser control is intentionally CLI-first for low context overhead.
	// Add native pi tools here later if the agent needs richer UI rendering.
}
