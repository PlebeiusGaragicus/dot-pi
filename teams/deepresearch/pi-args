# Optional default CLI flags for `p deepresearch` (read by bash_aliases). One flag per line; # starts a comment.
#
# IMPORTANT: must end with a newline (this comment also works) or last line will be ignored
