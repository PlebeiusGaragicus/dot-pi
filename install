#!/usr/bin/env bash
set -euo pipefail

cat <<'EOF'
The dot-pi curl installer has been removed.

Install dot-pi with Pi's package manager instead:

  pi install git:https://github.com/PlebeiusGaragicus/dot-pi

Update with:

  pi update

See docs/install.md for details.
EOF

exit 1
