# Subdirectories to pre-create in each workspace run.
WORKSPACE_DIRS=".browser-control sessions"

# Environment passed to pi on fresh launch and resume.
BROWSER_CONTROL_STATE_DIR="$WORKSPACE_DIR/.browser-control"
